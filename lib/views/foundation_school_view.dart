import 'package:flutter/material.dart';

class FoundationSchoolView extends StatelessWidget {
  const FoundationSchoolView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Foundation School Content")),
    );
  }
}
